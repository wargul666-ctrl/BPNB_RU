<form class="custom-dialog defend-dialog">
  <header class="dialog-header">
    <h2>{{label}}</h2>
  </header>

  <input
    type="hidden"
    name="rollLabel"
    value="{{label}}"
    data-dtype="String"
  />

  <div class="form-group">
    <label>{{label}}:</label>
    <input
      type="text"
      name="rollFormula"
      value="{{rollFormula}}"
      data-dtype="String"
    />
  </div>

  <div class="form-group">
    <label>{{localize "BPNB_BORG.Labels.DR"}}:</label>
    <input
      type="text"
      name="rollDRFormula"
      value="{{rollDRFormula}}"
      data-dtype="String"
    />
  </div>

  <footer class="dialog-buttons">
    <button type="submit" class="dialog-button roll">Бросить</button>
    <button type="button" class="dialog-button cancel" data-action="close">Отмена</button>
  </footer>
</form>
